#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
int ch;
for (ch = '0'; ch <= '50'; ch++)
{
putchar(ch);
}
for (ch = '51'; ch <= '99'; ch++)
{
putchar(ch)
}
putchar(',');
putchar("");
return (0);
}
